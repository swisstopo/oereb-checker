<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>ch.swisstopo.oerebchecker</groupId>
    <artifactId>oereb-checker</artifactId>
    <version>1.0</version>
    <packaging>jar</packaging>

    <developers>
        <developer>
            <organization>Geocloud AG</organization>
            <organizationUrl>https://www.geocloud.ch</organizationUrl>
        </developer>
    </developers>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <java.version>21</java.version>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <maven.plugins.version>3.7.1</maven.plugins.version>
        <software.amazon.awssdk.version>2.41.10</software.amazon.awssdk.version>
        <com.google.code.gson.version>2.13.2</com.google.code.gson.version>
        <jakarta.xml.bind.version>4.0.0</jakarta.xml.bind.version>
        <jakarta.xml.ws.version>4.0.0</jakarta.xml.ws.version>
        <jakarta.jws.version>3.0.0</jakarta.jws.version>
        <com.sun.xml.ws.version>4.0.2</com.sun.xml.ws.version>
        <org.verapdf.version>1.28.2</org.verapdf.version>
        <org.slf4j.version>2.0.17</org.slf4j.version>
        <org.junit.jupiter.version>6.0.0</org.junit.jupiter.version>
        <org.jsoup.version>1.21.2</org.jsoup.version>
    </properties>

    <dependencies>
        <dependency> <!-- https://mvnrepository.com/artifact/software.amazon.awssdk/auth -->
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>auth</artifactId>
            <version>${software.amazon.awssdk.version}</version>
        </dependency>
        <dependency> <!-- https://mvnrepository.com/artifact/software.amazon.awssdk/aws-core -->
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>aws-core</artifactId>
            <version>${software.amazon.awssdk.version}</version>
        </dependency>
        <dependency> <!-- https://mvnrepository.com/artifact/software.amazon.awssdk/core -->
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>core</artifactId>
            <version>${software.amazon.awssdk.version}</version>
            <type>pom</type>
        </dependency>
        <dependency> <!-- https://mvnrepository.com/artifact/software.amazon.awssdk/regions -->
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>regions</artifactId>
            <version>${software.amazon.awssdk.version}</version>
        </dependency>
        <dependency> <!-- https://mvnrepository.com/artifact/software.amazon.awssdk/s3 -->
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>s3</artifactId>
            <version>${software.amazon.awssdk.version}</version>
        </dependency>
        <dependency> <!-- https://mvnrepository.com/artifact/software.amazon.awssdk/utils -->
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>utils</artifactId>
            <version>${software.amazon.awssdk.version}</version>
        </dependency>

        <dependency>
            <groupId>com.amazonaws</groupId>
            <artifactId>aws-lambda-java-core</artifactId>
            <version>1.4.0</version>
        </dependency>
        <dependency>
            <groupId>com.amazonaws</groupId>
            <artifactId>aws-lambda-java-runtime-interface-client</artifactId>
            <version>2.8.7</version>
        </dependency>
        <dependency>
            <groupId>com.amazonaws</groupId>
            <artifactId>aws-lambda-java-events</artifactId>
            <version>3.16.1</version>
        </dependency>
        <dependency>
            <groupId>com.amazonaws</groupId>
            <artifactId>aws-lambda-java-log4j2</artifactId>
            <version>1.6.0</version>
        </dependency>

        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-core</artifactId>
            <version>2.25.2</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-api</artifactId>
            <version>2.25.2</version>
        </dependency>
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-layout-template-json</artifactId>
            <version>2.25.2</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-slf4j2-impl</artifactId>
            <version>2.25.2</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
            <version>${org.slf4j.version}</version>
        </dependency>

        <dependency>
            <groupId>com.google.code.gson</groupId>
            <artifactId>gson</artifactId>
            <version>${com.google.code.gson.version}</version>
        </dependency>

        <dependency>
            <groupId>jakarta.xml.bind</groupId>
            <artifactId>jakarta.xml.bind-api</artifactId>
            <version>${jakarta.xml.bind.version}</version>
        </dependency>
        <dependency>
            <groupId>jakarta.xml.ws</groupId>
            <artifactId>jakarta.xml.ws-api</artifactId>
            <version>${jakarta.xml.ws.version}</version>
        </dependency>
        <!-- jakarta.jws-api removed: all its classes are part of jakarta.xml.ws-api:4.0.0 -->
        <!-- JAX-WS Runtime implementation (needed to actually run the soap calls) -->
        <dependency>
            <groupId>com.sun.xml.ws</groupId>
            <artifactId>jaxws-rt</artifactId>
            <version>${com.sun.xml.ws.version}</version>
            <exclusions>
                <!-- com.sun.xml.bind:jaxb-impl is the old groupId for the same artifact
                     as org.glassfish.jaxb:jaxb-runtime (also pulled in by jaxws-rt).
                     Keeping both causes 641 overlapping classes in the shade JAR. -->
                <exclusion>
                    <groupId>com.sun.xml.bind</groupId>
                    <artifactId>jaxb-impl</artifactId>
                </exclusion>
                <!-- com.sun.xml.bind:jaxb-core duplicates org.glassfish.jaxb:jaxb-core -->
                <exclusion>
                    <groupId>com.sun.xml.bind</groupId>
                    <artifactId>jaxb-core</artifactId>
                </exclusion>
                <!-- jakarta.jws classes are already included in jakarta.xml.ws-api:4.0.0 -->
                <exclusion>
                    <groupId>jakarta.jws</groupId>
                    <artifactId>jakarta.jws-api</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <!-- https://docs.verapdf.org/develop/ -->
        <dependency>
            <groupId>org.verapdf</groupId>
            <artifactId>validation-model-jakarta</artifactId>
            <version>${org.verapdf.version}</version>
        </dependency>
        <dependency>
            <groupId>org.verapdf</groupId>
            <artifactId>parser</artifactId>
            <version>${org.verapdf.version}</version>
        </dependency>
        <dependency>
            <groupId>org.verapdf</groupId>
            <artifactId>pdf-model</artifactId>
            <version>${org.verapdf.version}</version>
        </dependency>
        <dependency>
            <groupId>org.verapdf</groupId>
            <artifactId>core-jakarta</artifactId>
            <version>${org.verapdf.version}</version>
        </dependency>
        <dependency>
            <groupId>org.verapdf</groupId>
            <artifactId>feature-reporting-jakarta</artifactId>
            <version>${org.verapdf.version}</version>
        </dependency>
        <dependency>
            <groupId>org.verapdf</groupId>
            <artifactId>wcag-validation-jakarta</artifactId>
            <version>${org.verapdf.version}</version>
            <exclusions>
                <!-- verapdf incorrectly declares jacoco-maven-plugin as a compile
                     dependency (it's a Maven plugin, not a runtime library). Without
                     this exclusion, JaCoCo + its transitive deps (ASM, Maven artifacts,
                     Plexus, ...) end up in the production uber-JAR. -->
                <exclusion>
                    <groupId>org.jacoco</groupId>
                    <artifactId>jacoco-maven-plugin</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>org.verapdf</groupId>
            <artifactId>metadata-fixer-jakarta</artifactId>
            <version>${org.verapdf.version}</version>
        </dependency>

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${org.junit.jupiter.version}</version>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>org.jsoup</groupId>
            <artifactId>jsoup</artifactId>
            <version>${org.jsoup.version}</version>
        </dependency>
    </dependencies>

    <profiles>
        <profile>
            <id>lambda</id>
            <build>
                <resources>
                    <resource>
                        <directory>src/main/resources-lambda</directory>
                    </resource>
                    <resource>
                        <directory>src/main/resources</directory>
                        <excludes>
                            <exclude>log4j2.xml</exclude>
                        </excludes>
                    </resource>
                </resources>
            </build>
        </profile>
    </profiles>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <compilerArgs>
                        <!-- The generated WSDL stub (PublicServices.java) uses APIs that are
                             deprecated in newer JAX-WS versions. We cannot change generated
                             code, so we suppress the deprecation note globally. -->
                        <arg>-Xlint:-deprecation</arg>
                    </compilerArgs>
                </configuration>
            </plugin>
            <plugin>
                <groupId>com.sun.xml.ws</groupId>
                <artifactId>jaxws-maven-plugin</artifactId>
                <version>4.0.2</version>
                <executions>
                    <execution>
                        <id>wsimport-partner</id>
                        <goals>
                            <goal>wsimport</goal>
                        </goals>
                        <configuration>
                            <wsdlDirectory>src/main/resources/ch/swisstopo/bfs</wsdlDirectory>
                            <wsdlFiles>
                                <wsdlFile>Prod_PartnerServices.svc.wsdl</wsdlFile>
                            </wsdlFiles>
                            <packageName>ch.swisstopo.oerebchecker.utils.uid</packageName>
                            <sourceDestDir>${project.build.directory}/generated-sources/wsimport</sourceDestDir>
                            <args>
                                <!-- Resolve naming collisions automatically -->
                                <arg>-B-XautoNameResolution</arg>
                                <!-- Override imported WSDL/XSD locations -->
                                <arg>-catalog</arg>
                                <arg>${project.basedir}/src/main/resources/ch/swisstopo/bfs/wsimport-catalog.xml</arg>
                                <!-- Make generated wsdlLocation portable -->
                                <arg>-wsdllocation</arg>
                                <arg>ch/swisstopo/bfs/Prod_PartnerServices.svc.wsdl</arg>
                            </args>
                            <!-- 2. Allow the plugin to download the XSDs via HTTPS -->
                            <vmArgs>
                                <vmArg>-Djavax.xml.accessExternalSchema=all</vmArg>
                            </vmArgs>
                        </configuration>
                    </execution>

                    <execution>
                        <id>wsimport-public</id>
                        <goals>
                            <goal>wsimport</goal>
                        </goals>
                        <configuration>
                            <wsdlDirectory>src/main/resources/ch/swisstopo/bfs</wsdlDirectory>
                            <wsdlFiles>
                                <wsdlFile>Prod_PublicServices.svc.wsdl</wsdlFile>
                            </wsdlFiles>
                            <packageName>ch.swisstopo.oerebchecker.utils.uid</packageName>
                            <sourceDestDir>${project.build.directory}/generated-sources/wsimport</sourceDestDir>
                            <!-- 1. Resolve naming collisions automatically -->
                            <args>
                                <arg>-B-XautoNameResolution</arg>
                                <!-- Override imported WSDL/XSD locations -->
                                <arg>-catalog</arg>
                                <arg>${project.basedir}/src/main/resources/ch/swisstopo/bfs/wsimport-catalog.xml</arg>
                                <!-- Make generated wsdlLocation portable -->
                                <arg>-wsdllocation</arg>
                                <arg>ch/swisstopo/bfs/Prod_PublicServices.svc.wsdl</arg>
                            </args>
                            <!-- 2. Allow the plugin to download the XSDs via HTTPS -->
                            <vmArgs>
                                <vmArg>-Djavax.xml.accessExternalSchema=all</vmArg>
                            </vmArgs>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.5.0</version>
                <configuration>
                    <createDependencyReducedPom>false</createDependencyReducedPom>
                    <shadedArtifactAttached>false</shadedArtifactAttached>
                    <shadedClassifierName/>
                    <promoteTransitiveDependencies>true</promoteTransitiveDependencies>
                    <transformers>
                        <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                            <mainClass>ch.swisstopo.oerebchecker.OerebChecker</mainClass>
                        </transformer>
                        <!-- This merges the META-INF/services files so all libraries work -->
                        <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer"/>
                        <transformer implementation="com.github.edwgiz.maven_shade_plugin.log4j2_cache_transformer.PluginsCacheFileTransformer" />
                        <!-- Add this to merge multi-release JAR info (fixes the module-info warnings) -->
                        <transformer implementation="org.apache.maven.plugins.shade.resource.ApacheLicenseResourceTransformer"/>
                        <transformer implementation="org.apache.maven.plugins.shade.resource.ApacheNoticeResourceTransformer"/>
                    </transformers>
                    <filters>
                        <filter>
                            <artifact>*:*</artifact>
                            <excludes>
                                <exclude>module-info.class</exclude>
                                <exclude>META-INF/*.SF</exclude>
                                <exclude>META-INF/*.DSA</exclude>
                                <exclude>META-INF/*.RSA</exclude>
                                <exclude>META-INF/LICENSE*</exclude>
                                <exclude>META-INF/NOTICE*</exclude>
                                <exclude>META-INF/DEPENDENCIES</exclude>
                                <!-- Maven POM metadata: not needed at runtime -->
                                <exclude>META-INF/maven/**</exclude>
                                <!-- Netty embeds this in every module jar; only one copy is needed -->
                                <exclude>META-INF/io.netty.versions.properties</exclude>
                                <!-- Plexus component descriptors from Maven build-tool deps -->
                                <exclude>META-INF/plexus/**</exclude>
                            </excludes>
                        </filter>
                        <filter>
                            <artifact>com.amazonaws:aws-lambda-java-runtime-interface-client</artifact>
                            <includes>
                                <include>**</include>
                            </includes>
                        </filter>
                    </filters>
                </configuration>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                    </execution>
                </executions>
                <dependencies>
                    <dependency>
                        <groupId>com.github.edwgiz</groupId>
                        <artifactId>maven-shade-plugin.log4j2-cachefile-transformer</artifactId>
                        <version>2.15.0</version>
                    </dependency>
                </dependencies>
            </plugin>
        </plugins>
    </build>
</project>